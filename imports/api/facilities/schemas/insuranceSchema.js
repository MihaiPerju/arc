import SimpleSchema from 'simpl-schema';

export default new SimpleSchema({
    insName: {
        type: String
    },
    insCode: {
        type: String
    },
    insBal: {
        type: String
    }
});